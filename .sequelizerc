const path = require("path");

module.exports = {
  config: path.resolve("./src/Config", "sequelize_config.json"),
  "seeders-path": path.resolve("./src", "seeders"),
  "migrations-path": path.resolve("./src", "migrations"),
};
